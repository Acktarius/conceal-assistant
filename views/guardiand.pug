extends layout

block layout-content
  div.View.WelcomeView
    h1.Banner Conceal Assistant
    form(action="/guardiand" method="POST")
      div.Message
       div.Title
        h1 Node Assistant
        span.Details Node is
        -var gs = guardianstatus
        -if (gs === 'active')
         h3 active
         button(type="submit") deactivate
        -else
         h4 not active  
    div.NavButtons
      a(href="/logout")
          div.NavButton Log Out